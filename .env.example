# Copy to .env.local for local dev. NEXT_PUBLIC_* vars are exposed to the browser.

# V2 editor is now the default. Set this to 1/true/yes/on to fall back to legacy V1.
# NEXT_PUBLIC_EDITOR_V1_FALLBACK=
