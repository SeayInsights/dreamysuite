"use client";

export function MotionTab() {
	return (
		<div className="p-4 text-sm text-muted-foreground">
			Motion presets (reveal on scroll, hover emphasis, reduced-motion)
			configure per-block in Phase 6.
		</div>
	);
}
