"use client";

export function ContentTab() {
	return (
		<div className="p-4 text-sm text-muted-foreground">
			Content fields (headings, copy, media) wire up once the shared site
			renderer is in place.
		</div>
	);
}
