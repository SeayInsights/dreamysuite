"use client";

export function LayoutTab() {
	return (
		<div className="p-4 text-sm text-muted-foreground">
			Layout controls (padding, width, alignment) arrive in Phase 3 once block
			schemas are consolidated.
		</div>
	);
}
